# ТЗ №3 — векторы: жёсткий грузовик (`rigid`, Motorwagen)

> Одно из трёх независимых заданий на графику грузовика (`LKWkalk-ku5`). Соседние:
> `…-brief-semitrailer.md`, `…-brief-swapbody.md`. Дизайн-контекст:
> `2026-07-27-truck-frame-scale-design.md`. Задания самодостаточны — читать соседние не нужно.

## Что рисуем и подо что

Одиночный грузовик: кабина и грузовой кузов на одной раме, без сцепки. Отличие от полуприцепа видно
сразу — кабина стоит **целиком впереди** кузова, а не подныривает под его переднюю грань.

Готового пресета под этот тип сегодня нет: он нужен для варианта «Eigene Maße» (произвольные размеры,
где тип выбирается вручную) и станет обязательным для автопоезда `тягач+прицеп` (`LKWkalk-p3p`, два
грузовых отсека) — там передняя секция это ровно такой грузовик.

**Итог задания — два файла:**

| файл | что на нём |
|---|---|
| `truck-rigid-side.svg` | вид сбоку: кабина, кузов, передняя и задняя оси |
| `truck-rigid-top.svg` | вид сверху: кабина с зеркалами, кузов, задние фитинги |

## Зачем переделываем

Нынешний рисунок нарисован намеренно не в масштабе — в его собственном описании написано *«minimized
cab and wheels»*. Приложение вставило его в систему координат, где груз строго 1:1, и картинка стала
врать глазу: при высоте кузова 2700 мм колесо выходит ~530 мм вместо ~1050, а пол стоит в ~370 мм над
землёй вместо ~1150.

---

# Процесс: четыре шага

**Шаг 1 — ИИ рисует растровое техническое изображение.** Чёрные линии на белом, без подписей и
размеров, не слишком детально. Оба вида (сбоку и сверху) — **на одной картинке**, друг под другом,
выровненные по передней грани.

**Шаг 2 — правки.** Смотрим результат, правим промптом, повторяем, пока пропорции и силуэт не сойдутся.
Проверять по отношениям: на растре нет миллиметров, но отношения меряются линейкой прямо на экране.

**Шаг 3 — трассировка утверждённой картинки в вектор** по математическим правилам. По ним приложение
находит кузов, растягивает его под любую машину и стыкует с грузом.

**Шаг 4 — сверка готового SVG по числам и повтор.** Прогнать список проверок; при любом расхождении
вернуться на тот шаг, который его породил, и пройти цепочку заново. Файл считается готовым, только
когда все проверки шага 4 сходятся — «на глаз похоже» здесь не работает: приложение поставит рядом
поддон, отмеренный до миллиметра, и любое расхождение станет видно.

Шаг 1 определяет всё. Ошибку пропорций или заполненный груз внутри кузова на шаге 3 уже не исправить —
поэтому условия ниже входят в **первый** промпт, а не добавляются потом.

---

## Шаг 1 — что должно быть на картинке

### Обязательные условия (без них картинку придётся переделывать)

1. **Грузовая область пуста.** Кузов — это РАЗРЕЗ, мы смотрим внутрь. Внутри не должно быть ничего:
   ни груза, ни поддонов, ни ящиков, ни ремней, ни дуг тента, ни внутренних стоек, ни штриховки, ни
   заливки. Туда приложение само положит груз — и всё, что нарисовано внутри, окажется под ним.
2. **Ничто не перекрывает кузов.** Кабина, крылья, отбойник, зеркала не заходят внутрь прямоугольника
   грузового объёма и не пересекают его границ. Всё либо строго снаружи, либо заканчивается на грани.
   Под полом — можно.
3. **Середина кузова — пустая плоскость.** Приложение растягивает кузов под длину машины. Всё, что
   нарисовано вдоль его середины (рама, боковой профиль, полосы, кронштейны), при растяжении
   порвётся. Для этого типа условие самое коварное: у жёсткого грузовика рама визуально непрерывна от
   кабины до кормы, но растягивать её нельзя. Детали — только у передней и задней граней.
4. **Два вида на одной картинке, строго 1:1 между собой.** Вид сбоку сверху листа, вид сверху под
   ним; одинаковая длина кузова, передняя грань точно одна под другой. Один рисунок гарантирует
   единый масштаб — два отдельных заказа его не гарантируют никогда.
5. **Строго ортогональная проекция.** Никакой перспективы, ракурса три четверти, наклона, теней,
   бликов, градиентов и объёма.
6. **Перёд слева** на обоих видах.
7. **Кабина целиком перед кузовом** — этим тип и отличается от полуприцепа.
8. **Ни одной надписи, цифры, стрелки, размерной линии, логотипа, номера.**
9. **Не слишком детально.** Узнаваемый силуэт: колёса с простыми дисками, окно, дверь, крылья, бак,
   задний отбойник. Без заклёпок, решётки радиатора, протектора, фар в деталях — на трассировке
   мелочь превращается в кашу из тысяч узлов.
10. **Чёрные замкнутые линии ровной толщины на чистом белом фоне.** Без серых, без штриховки, без
    текстур: трассировка честно повторит любую грязь.

### Заготовка промпта

```
Technical line drawing of a European rigid truck (Motorwagen): a cab and a box
body on one chassis, no trailer, no articulation. The cab stands entirely ahead
of the cargo body. Two orthographic views in one image, stacked vertically and
aligned: side view on top, top view below, both facing left, exactly the same
scale and the same body length.

Pure black outlines on a plain white background. Uniform line weight. No
perspective, no 3/4 angle, no shading, no gradients, no shadows, no colour.
No text, no numbers, no dimension lines, no arrows, no logos, no licence plates.

The cargo body must be drawn as an EMPTY open frame: only its outline, nothing
inside it, no cargo, no pallets, no straps, no internal posts, no hatching, no
fill. Nothing may overlap or cross into the cargo body area. The middle section
of the body must stay a plain empty surface with no details along it — all
details belong near the front face or the rear face only. Do not draw a
continuous chassis rail running the whole length of the body.

Realistic proportions: wheel diameter 0.39 of the body height, body floor sits
0.43 of the body height above the ground, body length 2.65 times the body
height, cab extends 0.89 of the body height ahead of the body front face.

Simple and readable, not highly detailed: wheels with plain discs, cab window,
door, mudguards, fuel tank, front steer axle, rear axle, rear underrun bar.
```

## Шаг 2 — по каким числам проверять растр

На картинке нет миллиметров, поэтому мерим **отношения к высоте кузова** — их видно линейкой на
экране. Ориентиры реальные; правь, если из практики знаешь точнее.

| что мерим | реально | доля от высоты кузова |
|---|---|---|
| диаметр колеса | ~1050 мм | **0,39** |
| пол кузова над землёй | ~1150 мм | **0,43** |
| кабина впереди передней грани | 2300–2500 мм | **0,85–0,93** |
| длина кузова (рисуем 7,15 м) | 7150 мм | **2,65** |
| шаг осей задней тележки | ~1350 мм | 0,50 |
| ширина по зеркалам (вид сверху) | ~2550 мм | — |
| ширина кузова (вид сверху) | 2450 мм | — |

Отдельно проверить глазами: внутри кузова пусто, ничто в него не залезает, середина без деталей, оба
вида одной длины и передние грани совпадают.

## Шаг 3 — трассировка: правила, по которым рисунок ляжет в приложение

### Цвета: ровно два, без полутонов

Трассируем в два цвета, без сглаживания в серое. Линии — заливка `#111111`, «вырезы» (диски колёс,
стекло) — заливка `#ffffff`. Третьего цвета, серых промежуточных и полупрозрачности быть не должно:
приложение перекрашивает `#111111` в свой токен, и всё, что не попало в эти два цвета, переживёт
перекраску и сломает чёрно-белую печать.

Результат трассировки — залитые контуры (`fill`), а не штрихи (`stroke`); это нормально и ожидаемо.
Белый прямоугольник фона холста, который трассировщик часто выдаёт первым объектом, удалить.

### Масштаб: 1 единица `viewBox` = 1 мм

После трассировки отмасштабировать весь рисунок целиком по одному известному размеру, затем
проверить вторым:

1. масштабируем так, чтобы диаметр колеса стал ровно **1050** единиц;
2. проверяем: от низа кузова до линии земли должно выйти около **1150**.

Приложение не пересчитывает пропорции — оно только двигает группы к граням кузова. Что в файле, то и
на экране рядом с поддонами.

### Три группы с обязательными id

```
<svg viewBox="0 0 W H">          W,H — в мм, с запасом вокруг машины
  <rect id="cargo-box" .../>     ПЛЕЙСХОЛДЕР кузова: пустой прямоугольник
  <g id="front"> … </g>          кабина целиком, рулевая ось
  <g id="rear">  … </g>          задняя ось/тележка, отбойник, брызговик, задние фитинги
</svg>
```

- **`cargo-box`** дорисовывается вручную поверх трассировки: точный прямоугольник по внутренним
  граням грузового объёма. Он задаёт систему координат — приложение читает его `x/y/width/height` и
  по ним понимает, где перёд, зад, пол и потолок. Сам он не рисуется, приложение его стирает.
- **`front`** прижимается к передней грани кузова, **`rear`** — к задней.
- Раму и боковой отбойник обрезать и положить в `rear`.
- **Обнулить трансформации:** никаких `transform="translate(...)"` на группах и на корне, координаты
  в файле абсолютные. Иначе приложение сдвинет группу второй раз поверх твоего сдвига.
- `viewBox` начинается с `0 0`.

## Шаг 4 — сверка готового SVG и повтор

Проверки по готовому файлу. Мимо хоть одной — возврат на указанный шаг и цепочка заново.

| # | что проверяем | норма | не сошлось → шаг |
|---|---|---|---|
| 1 | диаметр колеса в единицах `viewBox` | 1050 ± 30 | 3 (пересчитать масштаб) |
| 2 | от низа `cargo-box` до линии земли | 1150 ± 50 | 2 (пропорции растра) |
| 3 | кабина впереди передней грани | 2300–2500 | 2 |
| 4 | ширина `cargo-box` на виде сверху | 2450 | 3 |
| 5 | длина `cargo-box` в двух файлах | **совпадает точно** | 1 (виды рисовать одной картинкой) |
| 6 | x передней грани `cargo-box` в двух файлах | **совпадает точно** | 1 |
| 7 | объекты внутри `cargo-box` | **ни одного** | 1 |
| 8 | части `front`/`rear`, заходящие за грань кузова | только ниже линии пола | 1 |
| 9 | габарит `rear` не начинается раньше | середины длины `cargo-box` | 3 (перерезать группы) |
| 10 | цвета в файле | только `#111111` и `#ffffff` | 3 (трассировать в 2 цвета) |
| 11 | прозрачность, градиенты, серые | **отсутствуют** | 3 |
| 12 | `transform` на корне и на группах | **отсутствуют** | 3 |
| 13 | `viewBox` начинается с `0 0` | да | 3 |
| 14 | толщина основного контура | 25–40 единиц | 3 (или 1, если линии неровные) |
| 15 | колесо после упрощения узлов | окружность, не многоугольник | 3 (меньше упрощать) |

Финальная мысленная проверка: растянуть `cargo-box` вдвое по длине. Кабина и оси не деформируются,
просто разъезжаются. Если что-то «между ними» рвётся — оно лежит не в той группе, возврат на шаг 3.
